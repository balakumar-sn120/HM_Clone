import React from "react";

function HMButton({ btnName }) {
  return (
    <button class="bg-white mr-1 text-black px-2 py-1 font-medium">
      {btnName}
    </button>
  );
}

export default HMButton;
